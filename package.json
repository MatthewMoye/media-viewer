{
  "name": "media-viewer",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "api": "npm --prefix api run",
    "ui": "npm --prefix ui run",
    "install:all": "npm install && npm install --prefix api && npm install --prefix ui",
    "dev": "concurrently --raw \"npm --prefix api run dev\" \"npm --prefix ui run dev\"",
    "start": "concurrently --raw \"npm --prefix api run dev\" \"npm --prefix ui run host\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
